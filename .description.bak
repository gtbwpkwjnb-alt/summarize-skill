---
name: session-summarize
description: 会话级全维总结 — 项目·进度·建议·错误，一个命令全清 | Session-level summary for project/progress/suggestion/error
---

---
name: session-summarize
version: "6.1.0"
user-invocable: true
trigger: ["总结", "summarize"]
description: 会话级全维总结 — 项目·进度·建议·错误，一个命令全清 | Session-level summary for project/progress/suggestion/error
---

# session-summarize v6.1 — 📋 项目总结 · 进度总结 · 建议总结 · 错误总结

> **定位**: 一个命令看清会话全貌——四个维度，20行内，无冗余
> **设计目标**: 轻量、精确、可执行，不给 token 留积压
> **核心原则**: 一句话能说清绝不用两句。增量必控，超限必缩。

---

## 触发方式（严格精确匹配）

**仅当输入精确等于以下字符串时触发**，句中包含、前缀匹配、相似变体均不触发：

| 输入 | 触发 | 说明 |
|------|:----:|------|
| `总结` | ✅ | 中文触发词，独立精确匹配 |
| `summarize` | ✅ | 英文触发词（向后兼容），独立精确匹配 |
| `总结 统计` | ✅ | 统计子命令 |
| `总结 压缩` | ✅ | 压缩子命令 |
| `总结 知识库` | ✅ | 知识库子命令 |
| `总结 台账` | ✅ | 台账子命令 |
| `总结 遗忘` | ✅ | 遗忘子命令 |
| `总结 深度 链` | ✅ | 因果链分析 |
| `总结 深度 验证` | ✅ | 出站验证 |
| `总结 深度 自检` | ✅ | 对抗性自检 |
| `总结 深度 写入` | ✅ | 自编辑规则文件 |
| 句中包含如``总结一下`` / ``帮我总结`` | ❌ | 不触发 |
| 英文句中 ``summarize this`` | ❌ | 不触发 |
| 前缀 ``总`` / ``sum`` | ❌ | 不触发 |

> 平台层须做精确字符串匹配，确保 `总结` 不与用户普通用词冲突。

---

## 输出精炼规则

整体输出硬上限 ≤20 行，超出按优先级折叠：

```
保底优先级（下>上）: 模块2 > 模块1 > 模块4 > 模块3
```

| 条目 | 上限 | 超出处理 |
|------|:----:|---------|
| 文件清单 | 8个 | `…等{N}个文件` |
| 关键决策 | 5条 | `…等{N}条决策` |
| 失败列表 | 5条 | `…等{N}条失败` |

**归档**：完整内容写入归档文件（覆盖更新），不加载到上下文。

---

## 存储层级 — 数据文件位置

**基址**：所有 harvest 文件集中存储在 Reasonix 用户数据目录下：

| 平台 | 基址路径 |
|:----:|---------|
| **Windows** | `%APPDATA%/reasonix/harvests/` |
| **macOS/Linux** | `~/.local/share/reasonix/harvests/` 或 `$XDG_DATA_HOME/reasonix/harvests/` |
| **回退** | `<project_root>/.reasonix/harvests/`（前两者均不可用时） |
| **检测方式** | 平台检测: Windows 查 `%APPDATA%`，Unix 查 `$XDG_DATA_HOME` 或 `~/.local/share` |

**目录结构**：

```
{基址}/
  {project}/                      ← 每个项目一个子目录
    {session-id}.md               ← 会话归档（覆盖更新，不加载上下文）
    failure-ledger.md             ← 失败台账（≤50条，按需检索）
    knowledge-base.md             ← 知识沉淀（≤50条，按需检索）
  error-ledger.md                 ← 冷区（≤100条，不加载上下文）
  index.md                        ← 收割索引（同会话只保留最新一条）
  archive/                        ← 压缩归档（显式恢复，永不自动加载）
    {date}-knowledge-compact.md
    {date}-failures-compact.md
```

**层级一览**：

| 层级 | 路径 | 上限 | 加载策略 |
|:----:|------|:---:|:--------:|
| 🔥**热区** | `$RULES_FILE` | ≤5条 | **每次会话 Read** |
| 🔶**记忆** | memory 系统 | ≤10条 | **每次会话加载** |
| 🔵**台账** | `{基址}/{project}/failure-ledger.md` | ≤50条 | 按需检索 |
| 📘**知识库** | `{基址}/{project}/knowledge-base.md` | ≤50条 | 按需检索 |
| ⬜**冷区** | `{基址}/error-ledger.md` | ≤100条 | 主动检索 |
| 📦**归档** | `{基址}/archive/` | 无上限 | 显式恢复 |

**常驻负担**：热区+记忆 ≤ **2KB**，超限告警。

**运行时**：每次 `总结` 执行时自动检测平台并设置 `$HARVEST_BASE`。文件不存在时自动创建。

---

## 边界场景与降级

| 场景 | 行为 | 输出 |
|------|------|------|
| harvests 基址不可写 | 输出警告，跳过归档，模块4降级为仅内存统计 | `⚠️ 归档不可写，跳过持久化` |
| 归档文件损坏 | 跳过该文件，重建空文件 | `⚠️ 归档损坏，已重建` |
| 写入权限不足 | 跳过所有写操作，仅输出内存数据 | `⚠️ 无写入权限，数据仅本次有效` |
| 平台自动检测失败 | 使用回退路径 `<project_root>/.reasonix/harvests/` | `ℹ️ 使用回退存储路径` |
| 旧版数据格式（v5） | 自动迁移到 v6 结构 | `↻ 数据格式已迁移 v5→v6` |
| 会话 ID 冲突 | 保留新归档，旧归档标 `[superseded]` | `↻ 会话已覆盖旧归档` |

**降级链**（按严重程度递增）：

```
正常 → 模块3跳过(建议总结) → 模块4跳过(错误总结) → 仅模块1+2 → 单行摘要
触发条件: 连续2次写失败、关键文件损坏、权限不足
```

---

## 模块 1：项目总结 — 发生了什么

```
📋 {项目} —— {1句总结}
> {3-5句事实摘要 — 每句一个独立事实，不含推测}

📁 文件({N}): {前8路径}, …等{N}个
🔑 决策({N}): {前5条 — 含"选A弃B理由"}, …等{N}条
```

🔄 变更:
  ✏️ 修改: {前3路径}
  ➕ 新增: {前2路径}
  🗑️ 删除: {前1路径}

---

## 模块 2：进度总结 — 做到哪了

```
✅ 完成({N}): {完成项简述}
⏳ 待办({N}): {待办项简述}
💡 下一步: {1-2条具体可执行的行动}
📊 ~{N}轮 ｜ 🟢{<50%} / 🟡{50-80%} / 🔴{>80%}
```

🎯 下次起点:
  └ {1句继续位置 + 注意前提}

---

## 模块 3：建议总结 — 怎么优化

### 3.1 链路优化（基于台账模式匹配）

| 检测模式 | 触发条件 | 优化建议 |
|---------|:--------:|---------|
| 重复构建失败 | 连续2+会话构建失败 | "建议 `go mod tidy` 前置" |
| 环境缺失 | 缺工具/依赖/服务 | "创建环境检查脚本" |
| 重复错误尝试 | 重试≥2次未换方向 | "设2次失败后自动换路" |
| 配置遗漏 | 缺 .env/配置 | "创建 .env.example + 校验" |
| 重复失败模式 | 台账同类≥3次 | "提升为项目约定" |
| 无效步骤 | 做了无用操作 | "改前先 Read 确认" |

```
⚡ 链路优化({N}):
  ├ {模式}: {1句建议}
  └ 依据: {N}次重复 | {示例}
```

### 3.2 知识沉淀

```
📌 知识沉淀({N}):
  🏗️ {架构决策} ｜ ⚠️ {坑点}→{方案} ｜ 🔧 {配置} ｜ 📐 {约定}
```

### 3.3 记忆健康度

```
📊 记忆健康度:
  🔥 规则(4/5 🟢) ｜ 🔶 记忆(3/10 🟢) ｜ 常驻≈1.5KB ✅
  🔵 台账(32/50 🟢) ｜ 📘 知识库(48/50 ⚠️)
  💡 建议 `总结 压缩` 节省 ~60% 空间
```

| 层级 | 健康标准 | 🟢 | 🟡 | 🔴 | 超限动作 |
|:----:|:--------:|:--:|:--:|:--:|---------|
| 热区规则 | ≤5条/≤2KB | <80% | 80-95% | >95% | 降级最旧1条 |
| 项目记忆 | ≤10条/≤2KB | <80% | 80-95% | >95% | forget 最久未引用2条 |
| 失败台账 | ≤50条 | <40 | 40-47 | ≥48 | 归档最旧20条 |
| 知识库 | ≤50条 | <40 | 40-47 | ≥48 | 去重合并归档 |

---

## 模块 4：错误总结 — 出了什么问题

### 失败6类

| 代码 | 类型 | 检测方式 |
|:----:|------|---------|
| TOOL_ERR | 工具报错 | API error / denied / timeout |
| EXEC_FAIL | 执行失败 | build error / test fail |
| ENV_MISS | 环境缺失 | 缺命令/依赖/服务 |
| CFG_MISS | 配置缺失 | 缺 .env/配置文件 |
| RETRY | 重复尝试 | 重试≥2次未换方向 |
| LOGIC | 逻辑偏差 | 用户纠正/无用功 |

### 输出

```
📊 失败({N}): {高频}({N}) | {最新1句}
| {简述} | 类型 | {N} | 状态 |
|-------|:----:|:---:|:----:|
| {前5条} | ENV_MISS | 3 | ✅ |

🛡️ 规则: {已采纳}/{总} ✅ ｜ {复发}/{总} ⚠️

💡 即时优化({N}):
  ├ {类型}: {具体修复}
  └ → {预期效果}

📏 规则定容:
  🔥 当前{4}条 ｜ clean最长: {规则}({N}天)
  💡 clean>30天未触发 → 建议降级
```

### 效果度量（跨会话追踪）

写入 `harvests/{project}/failure-ledger.md` 台账头部：

```
# 效果度量
| 指标 | 值 |
|------|:--:|
| 💡 链路优化建议 | {N}条 |
| ✅ 采纳 | {N}条 |
| ⚠️ 复发 | {N}条 |
| 📈 采纳率 | {N}% |
```

---

## 子命令

| 命令 | 功能 | 输出 |
|------|------|------|
| `总结 统计` | 全局统计 | `📊 运行{N}次 \| 采纳率{NN}% \| 复发率{NN}%` |
| `总结 压缩` | 压缩归档 | `📘 {N}→{N} \| 🔵 {N}→{N} \| 💾 ~{N}KB` |
| `总结 知识库` | 查看知识库 | `📚 {N}条 > {最近3条}` |
| `总结 台账` | 查看台账 | `📊 {N}条 > ✅{N} ⚠️{N}` |
| `总结 遗忘` | 清理记忆 | `📋 {N}条可清理 > 建议 forget 最久未引用的2条` |
| `总结 深度 链` | 因果链分析 | `🔗 A→B→C` |
| `总结 深度 验证` | 出站验证 | `🌐 参考: {URL}` |
| `总结 深度 自检` | 对抗性自检 | `✅ 通过 / ⚠️ 疑虑` |
| `总结 深度 写入` | 自编辑规则 | `Read→定位→插入→定容→commit` |

---

## 输出示例

### 示例1：重构会话

```
📋 MyProject —— JWT 认证重构为独立 middleware
> 将 JWT 验证从 handler 层抽出为 pkg/jwt/middleware
> 选 golang-jwt 而非 jose（社区更大，零外部依赖）

📁 文件(8): pkg/jwt/middleware.go, internal/handler/login.go, ...
🔑 决策(2): golang-jwt > jose; Wire DI > 手动注入

✅ 完成(3): middleware 提取、handler 简化、单元测试
⏳ 待办(1): Token 刷新 endpoint
💡 下一步: go test ./internal/...
📊 ~34轮 ｜ 🟢 42%

⚡ 链路优化(1):
  ├ 重复构建: 建议 go mod tidy 前置
  └ 依据: 2次因 go.sum 过期导致构建失败

📊 失败(2): ENV_MISS(2) | go test -count=1 避免缓存
🛡️ 规则: 2/2 ✅
```

### 示例2：调试会话

```
📋 MyProject —— 修复 WebSocket 连接泄漏
> 根因: 断开时未调用 conn.Close()
> 修复: defer conn.Close() 追加到 onDisconnect

📁 文件(3): internal/ws/handler.go
🔑 决策(1): 用 context.WithCancel 替代 manual flag

✅ 完成(1): 修复泄漏
⏳ 待办(0): —
💡 下一步: 观察生产环境连接数
📊 ~12轮 ｜ 🟢 25%

📌 知识沉淀(1):
  ⚠️ WebSocket Close 非自动，必须在 onDisconnect 中显式调用

📊 失败(0):
🛡️ 规则: — | 未触发
```

### 示例3：初始探索会话

```
📋 NewProject —— 初始化项目结构
> 用 Go 1.22 + gin 搭建 HTTP 服务骨架
> 选 PostgreSQL 而非 SQLite（生产环境需要）

📁 文件(15): cmd/server.go, internal/config/config.go, ...
🔑 决策(3): gin > echo; PostgreSQL > SQLite; Wire DI > manual

✅ 完成(5): 项目初始化、路由、配置、DB 连接、Dockerfile
⏳ 待办(2): CI/CD 配置、单元测试
💡 下一步: 编写 health check endpoint
📊 ~8轮 ｜ 🟢 15%

📊 失败(0):
🛡️ 规则: — | 未触发
```

---

## 版本历史

### v6.1.0（当前）
- 🏷️ 重命名: `summarize` → `session-summarize`，触发词保持 `总结` / `summarize` 不变
- 📝 新增: 边界场景与降级策略（文件损坏/写失败/权限不足等）
- 📋 新增: 3 个完整输出示例（重构/调试/初始探索）
- 📊 新增: 效果度量（采纳率/复发率追踪）
- 🐛 修复: 重复 frontmatter

### v6.0.0（2026-06-24）
- 🎯 全面重构: 四柱总结（项目·进度·建议·错误）
- 🔥 新增: 失败6类全类型收割（TOOL_ERR / EXEC_FAIL / ENV_MISS / CFG_MISS / RETRY / LOGIC）
- ⚡ 新增: 链路优化建议（6种失败模式→优化方案）
- 🛡️ 新增: 规则定容（热区≤5条，clean>30天自动降级）
- 📦 新增: 子命令压缩/遗忘/深度
- 📏 输出≤20行硬上限，增量必控超限必缩

### 版本语义
- `major` — 模块结构/存储格式破坏性变更
- `minor` — 新增功能模块或子命令
- `patch` — Bug 修复、文档完善、边界场景补充

---

## 格式原则

- **四柱清晰**：项目·进度·建议·错误，不混入无关内容
- **一行一事**：不堆砌无关信息
- **上限折叠**：超限自动折叠，不突破20行
- **增量必控**：每次输出检查所有层级是否超限
- **超限必缩**：超限时自动触发压缩建议
- **常驻可见**：热区+记忆标注 KB 估测
- **建议非待办**：用户自主选择是否采纳
