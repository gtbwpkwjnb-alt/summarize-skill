---
name: summarize
description: 在 Codex 中总结当前任务、进度、决策、验证和下一步，生成可交接的上下文，并基于本次证据给出操作、优化和行为建议。用户说“总结”“总结一下”“总结进度”“复盘”“交接”“handoff”或“summarize”时使用。默认只读；仅在用户明确说“总结 保存”时写入当前工作区。
license: MIT
---

# 会话总结 v10

生成短、可执行、可核验的任务状态，并把本次发现转化为下一步、优化与行为建议。不要重述对话或把不确定判断写成长期记录。

## 目标与边界

- 优先回答：现在处于什么状态、依据是什么、下一步由谁做什么、怎样避免同类问题再次发生。
- 默认只读取当前可见会话、工具结果和工作区状态；不要自动创建文件、更新计数器、归档错误或修改规则。
- 只在用户明确要求保存（例如 `总结 保存` / `summarize save`）时写入。
- 不要自行再次触发本技能，也不要因轮次、文件数或上下文长度自动总结。
- 不要创建或更新 Codex goal；只有用户明确要求时才使用 goal 相关能力。

## 命令与意图

| 用户表达 | 模式 | 产出 |
|---|---|---|
| `总结`、`总结一下`、`总结进度`、`复盘`、`summarize` | 标准 | 5–12 行状态报告，末尾含建议 |
| `总结 交接`、`handoff` | 交接 | 可直接粘贴到新任务的完整上下文 |
| `总结 诊断` | 诊断 | 标准报告 + 有证据的流程风险观察 |
| `总结 保存`、`summarize save` | 保存 | 先生成交接报告，再保存到当前项目 |
| `总结 恢复` | 恢复 | 读取并展示上次显式保存的交接报告 |

将自然语言请求按最接近的模式处理。用户同时要求保存和诊断时，先诊断，再将最终交接报告保存。

## 取证规则

1. 从当前可见会话提取目标、用户约束、已完成工作、阻塞项和用户尚未答复的问题。
2. 仅在需要核验工作区状态时使用只读工具：优先 `git status --short`、`git diff --stat`、必要的文件读取或测试输出。仓库不存在、命令失败或信息不在当前上下文时，明确写“未核验”或“不可得”。
3. 只在已有活跃 goal 且其状态与报告相关时读取它；不要为总结创建 goal。
4. 将每个事实标为以下之一：
   - **已核验**：有工具输出、文件内容或用户明确确认。
   - **会话陈述**：来自当前会话但未独立核验。
   - **未核验**：缺少可用证据；不要把它写成完成事实。
5. 不要编造文件变更、测试、命令结果、决定理由、跨会话历史或完整对话覆盖范围。

## 建议生成

每次总结都在输出末尾给出 `## 建议`。先读取 [references/recommendations.md](references/recommendations.md)，再从本次已核验事实和会话陈述中选择建议。

- 固定顺序为：**立即行动**、**优化建议**、**行为建议**。没有证据支持的类别隐藏。
- 每条建议使用“观察 → 影响 → 操作 → 验证”的最短闭环。操作必须具体到下一次动作、命令、检查项或需要用户决定的事项。
- 标准模式最多 2 条；交接模式最多 3 条；诊断模式最多 4 条。优先保留能降低风险或解除阻塞的建议。
- 同一问题在当前可见会话中出现至少两次时，必须给出根因定位或防重复建议；不要只写“注意”“加强”“优化一下”。
- 没有异常模式时，仍给出一条收束建议：按既定下一步执行，并在关键完成声明前补足验证。
- 建议是提案，不自动修改代码、配置、规则或任务目标。涉及写入、外部动作或范围扩大时，明确标为“需确认”。

## 输出规范

先给结果状态：`完成`、`进行中`、`受阻`、`待决策` 或 `未开始`。除非有相应验证证据，否则不要使用“已完成”或“已通过”。

以一行结果和一行“当前应做”开场；让用户在不展开全文时就看到状态与优先动作。按需显示以下区段；没有信息时隐藏整段：

```markdown
# {项目或任务} — {状态}
{一句话结果；只陈述有证据支撑的状态}

**当前应做：** {最高优先级动作；没有动作时写“等待用户决策：{问题}”}

## 已核验
- {事实}（证据：{命令、文件或工具结果}）

## 当前情况
- {会话陈述或未核验事项}

## 决策
- {决定} → 原因：{仅在会话中明确出现时写}

## 阻塞与风险
- {阻塞} → 需要：{具体输入、权限或操作}

## 下一步
1. {可执行动作；注明执行方或命令/路径}

## 继续任务提示
{仅交接模式：一段可直接粘贴的新任务提示，含目标、约束和下一步}

## 建议
- 立即行动：观察 {事实} → 影响 {风险或阻塞} → 操作 {具体动作} → 验证 {通过条件}
- 优化建议：观察 {重复模式} → 操作 {根治或预防措施} → 验证 {复发检查}
- 行为建议：观察 {流程缺口} → 操作 {下次遵循的工作方式} → 验证 {可见证据}
```

- `## 建议` 始终是最后一个内容区段。保存回执、如有，放在其前面。
- 简单请求保持在 5–8 行；一般任务不超过 12 行；交接或诊断最多 24 行。不要为了凑满建议类别而增加内容。
- 文件列表只列与下一步或验证有关的文件。优先使用相对路径；保存成功后才显示实际绝对保存路径。
- 用清晰标题、短列表和留白组织阅读；不要使用装饰图标、虚构状态徽章、冗余表格或“第 N 节”类标签。
- 将“无可核验变更”如实写出，不用空泛的“无问题”替代。

## 诊断模式

诊断是可选复盘，不是对 Agent 的自动判罚，也不修改任何规则或历史账本。只报告在当前可见证据中能成立的观察：

- 关键完成声明缺少相应验证输出；
- 同一失败被以相同方式重复尝试；
- 存在未落实的用户问题、待决策项或承诺；
- 修改了配置/规则类文件但当前会话中看不到授权证据。

每项都附证据位置和建议下一步，并汇入输出末尾的 `## 建议`。证据不足时写“无法判断”，不要标记为违规、复发或累计次数。不要自动修复、自动写入 `AGENTS.md`、`SKILL.md`、计数器或错误账本。

## 保存与恢复

保存时先读取 [references/operations.md](references/operations.md)。只有用户明确选择保存时：

1. 检查当前项目的工作区规则和可写位置。
2. 生成带 `summarize-handoff` 元数据的交接模式报告；不要保存原始对话、私密信息、未核验推测或工具密钥。
3. 使用 `scripts/save_handoff.py` 校验敏感信息、加锁、备份并原子替换 `{项目根目录}/.transfers/latest.md`；不能确定项目根目录时，停止并报告，不要写入技能目录或用户主目录。
4. 写入后读取该文件并运行只读校验；仅在回测成功时报告实际绝对路径。

恢复时只读取该路径并标明元数据是否完整。文件不存在时报告“未找到已保存的交接报告”，不要创建空文件。

## 兼容与迁移

- 在 Codex 中，遵守当前工作区 `AGENTS.md`、工具权限和会话可见性边界；它们优先于本技能。
- 旧版 `harvests/`、`_counters.md`、`error-ledger.md` 和三档模型规则只保留作历史数据；本版本不自动读取、更新或清理它们。
- 其他平台缺少工作区或工具可见性时，退化为基于当前对话的只读报告，并明确可见性限制。
